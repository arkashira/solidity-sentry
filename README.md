# Solidity Sentry
A tool for generating audit findings with explanations and remediation links.

## Usage
1. Create a `TemplateSystem` instance.
2. Create a `SoliditySentry` instance with the `TemplateSystem` instance.
3. Call the `generate_findings` method with a list of issues.

## Example
