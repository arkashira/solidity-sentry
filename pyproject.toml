[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "solidity-sentry"
version = "0.1.0"
description = "Solidity contract risk scoring tool"
readme = "README.md"
requires-python = ">=3.8"
